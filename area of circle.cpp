#include <iostream.h>
void computeCircle(double&, double&, double);
int main)(
{
double r, a, c;
cout << "Enter radius" :;
cin >> r;
computeCircle(a, c, r);
cout << "area = " << a << ", circumference = " << c << endl;
return 0;
}
